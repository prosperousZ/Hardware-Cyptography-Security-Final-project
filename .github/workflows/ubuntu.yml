name: Ubuntu

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: test AES
      run: pip3 install Pillow
      run: pip3 install pycryptodome
      run: cd src/AES; python3 ./main.py ../../images/photo.jpg

